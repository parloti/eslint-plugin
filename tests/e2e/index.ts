/** TODO: REMOVE - Marker declaration that keeps the e2e folder aligned with barrel-file rules. */
export interface EndToEndSpecsBarrel {
  /** Scope marker for the e2e barrel file. */
  readonly scope: "tests/e2e";
}
