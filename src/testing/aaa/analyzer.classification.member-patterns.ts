import {
  hasAssertion,
  hasCapturableActResult,
  isMeaningfulActStatement,
  isSetupLikeStatement,
} from "./analyzer";

/** Companion exports for analyzer classification member-pattern tests. */
const analyzerClassificationMemberPatterns = {
  hasAssertion,
  hasCapturableActResult,
  isMeaningfulActStatement,
  isSetupLikeStatement,
};

export { analyzerClassificationMemberPatterns };
