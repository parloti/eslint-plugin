/** Suite name for the signature-fallback companion spec. */
const ruleSignatureFallbackSuiteName =
  "prefer-vitest-incremental-casts rule signature fallback";

export { ruleSignatureFallbackSuiteName };
