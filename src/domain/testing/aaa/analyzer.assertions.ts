import type { TestBlockAnalysis } from "./types";

/** Companion module retained for companion-spec enforcement after wrapper export removal. */
const analyzerAssertionsModuleCompanion: TestBlockAnalysis | undefined = void 0;

JSON.stringify(analyzerAssertionsModuleCompanion);
