# ⚙️ Implementation: E2E Validation Follow-Up

**🐞 Bug:** [BUG-001](../product/bugs/bug-001-aaa-and-barrel-rule-regressions.md) - AAA and barrel rule regressions
**🏛️ Epic:** EPIC-UNKNOWN - Not provided
**🚀 Feature:** FEAT-UNKNOWN - Not provided
**🧾 User Story:** US-UNKNOWN - Not provided
**🕒 Created At:** 2026-03-29T22:44:55Z
**📌 Status:** Ready for review

---

## 🎯 Objective

Finish the residual-risk follow-up after enabling e2e discovery by fixing the newly exposed AAA and typed Vitest-cast regressions and re-running the repo validation pipeline.

---

## 🧱 Changes

- File: src/testing/enforce-aaa-phase-purity/phase-purity-reporting.ts
  - Limited `nonAssertionInAssert` reporting in combined `Act & Assert` flows to assertion statements only.
- File: src/testing/enforce-aaa-phase-purity/rule.spec.ts
  - Added clean combined `Act & Assert` coverage through a parameterized regression.
- File: src/testing/aaa/analyzer.classification.spec.ts
  - Added branch-coverage cases for declared assertions, wrapped assertions, and evaluated `assert.equal(...)` calls.
- File: tests/e2e/enforce-aaa-phase-purity.spec.ts
  - Updated stale expectations and kept the helper-driven utility scenario as an expected future failure.
- File: tests/e2e/prefer-vitest-incremental-casts.spec.ts
  - Aligned the e2e expectation with post-fix ESLint output and increased the local timeout for the slow typed suite.
- File: vitest.config.ts
  - Kept e2e `tests/**/*.spec.ts` discovery enabled while restoring test-file coverage exclusion.

---

## 💻 Code

```ts
if (
  purityState.allowsAssert &&
  purityState.containsAssertion &&
  !purityState.isValidAssert
) {
  context.report({ messageId: "nonAssertionInAssert", node: statement.node });
}
```

```ts
describe(
  "prefer-vitest-incremental-casts e2e",
  { timeout: Number.parseInt("30000", 10) },
  () => {
    // ...
  },
);
```

---

## 🧪 Notes

- Edge cases handled:
  - Valid combined `Act & Assert` flows no longer report `nonAssertionInAssert` on the Act statement.
  - Inline evaluation inside `expect(...)` remains invalid.
  - Slow typed e2e casts remain stable under full-suite load.
- Reproduction/regression checks:
  - Ran focused e2e verification for the touched AAA and incremental-casts suites.
  - Ran `npm run validate` twice successfully.
- Assumptions:
  - The helper-driven utility AAA case remains a documented future expectation rather than current supported behavior.
- Limitations:
  - The typed Vitest-cast tests are still slow; this pass stabilizes them with a local timeout rather than redesigning the typed fixture harness.
- Validation performed:
  - `npm run validate`
  - `npm run validate`

## 🔁 Handoff

- Next: Reviewer Agent / human
- Status: ready-for-handoff
