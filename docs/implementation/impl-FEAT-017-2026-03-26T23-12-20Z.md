# ⚙️ Implementation: FEAT-017 Review Fixes

**🏛️ Epic:** EPIC-017 - Adopt Updated Plugin Rule Specifications  
**🚀 Feature:** FEAT-017 - Align Rule Implementations To New Specs  
**🧾 User Story:** US-017 - Replace Current Rule Behavior With Updated Specifications
**🕒 Created At:** 2026-03-26T23:12:20Z
**📌 Status:** Ready for review

---

## 🎯 Objective

Address the post-implementation review findings for the barrel-rule alignment work by removing stale shared state, aligning barrel purity checks with configurable barrel stems, and simplifying e2e execution.

---

## 🧱 Changes

- File: src/architecture/consistent-barrel-files/consistent-barrel-files-listeners.ts
  - Removed module-scoped cache and de-duplication state.
  - Switched missing-barrel reporting to a deterministic per-directory primary file strategy.
- File: src/architecture/consistent-barrel-files/barrel-file-utilities.ts
  - Added directory module-file inspection helpers for deterministic primary-file selection.
- File: src/architecture/consistent-barrel-files/exports-only-rule.ts
  - Added `allowedBarrelNames` support so custom barrel stems are linted for purity.
- File: src/architecture/consistent-barrel-files/types.ts
  - Replaced `never` with an explicit option type for `barrel-files-exports-only`.
- File: src/architecture/consistent-barrel-files/exports-only-rule.spec.ts
  - Added coverage for custom barrel names.
- File: tests/e2e/barrel-files-exports-only.test.ts
  - Added end-to-end coverage for custom barrel names.
- File: tests/e2e/README.md
  - Updated the documented Vitest glob to `tests/e2e/*.test.ts`.
- File: tests/e2e/*.spec.ts
  - Removed obsolete import-only wrappers.

---

## 💻 Code

```ts
const primaryFilename = getPrimaryNonBarrelModuleFile(
  directory,
  options.allowedNamesSet,
);

if (primaryFilename !== path.basename(filename)) {
  return {};
}
```

```ts
const allowedBarrelNames = normalizeAllowedBarrelNames(
  rawOptions?.allowedBarrelNames,
);
```

---

## 🧪 Notes

- Edge cases handled: custom barrel stems such as `mod.ts`; repeated lint runs without stale shared cache state.
- Assumptions: `barrel-files-exports-only` should share the configurable barrel-name model used by the other barrel rules.
- Limitations: full `npm run validate` remains subject to the repo's existing global coverage threshold debt.
- Validation performed:
  - `npm test -- src/architecture/consistent-barrel-files/exports-only-rule.spec.ts src/architecture/consistent-barrel-files/exports-only-rule-test-utilities.spec.ts src/architecture/consistent-barrel-files/consistent-barrel-files-listeners.spec.ts tests/e2e/barrel-files-exports-only.test.ts tests/e2e/consistent-barrel-files.test.ts tests/e2e/no-reexports-outside-barrels.test.ts --coverage.enabled=false`
  - `npm run eslint -- src/architecture/consistent-barrel-files/barrel-file-utilities.ts src/architecture/consistent-barrel-files/consistent-barrel-files-listeners.ts src/architecture/consistent-barrel-files/exports-only-rule.ts src/architecture/consistent-barrel-files/exports-only-rule-test-utilities.ts src/architecture/consistent-barrel-files/exports-only-rule.spec.ts src/architecture/consistent-barrel-files/exports-only-rule-test-utilities.spec.ts src/architecture/consistent-barrel-files/types.ts tests/e2e/barrel-files-exports-only.test.ts vitest.config.ts`
  - `npm run typecheck`
  - `npm run typecheck:spec`

## 🔁 Handoff

- Next: Reviewer Agent / human
- Status: ready-for-handoff
