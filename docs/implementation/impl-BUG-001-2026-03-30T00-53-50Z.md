# ⚙️ Implementation: Review Follow-Up Test Tightening

**🐞 Bug:** [BUG-001](../product/bugs/bug-001-aaa-and-barrel-rule-regressions.md) - AAA and Barrel Rule Regressions
**🏛️ Epic:** EPIC-UNKNOWN - Not provided
**🚀 Feature:** FEAT-UNKNOWN - Not provided
**🧾 User Story:** US-UNKNOWN - Not provided
**🕒 Created At:** 2026-03-30T00:53:50Z
**📌 Status:** Ready for review

---

## 🎯 Objective

Address the remaining review findings by tightening the permissive AAA expectation tests, adding mirrored concurrency coverage for the e2e typed helper path, and revalidating the exact final workspace state.

---

## 🧱 Changes

- File: tests/e2e/enforce-aaa-phase-purity.spec.ts
  - Replaced permissive `it.fails(...)` cases with explicit current-behavior assertions.
  - The metadata-only self-test case now asserts the current `assertionOutsideAssert` diagnostic.
  - The helper-driven utility case now asserts the current `setupAfterAct` diagnostic.
- File: tests/e2e/prefer-vitest-incremental-casts.spec.ts
  - Added a mirrored concurrent `Promise.all(...)` regression so the e2e helper path proves the same queue-based isolation already covered in the unit helper spec.
- File: src/testing/prefer-vitest-incremental-casts/rule-test-helpers.spec.ts
  - Increased the fixed-output typed helper timeout to keep the test stable under the heavier full-suite load after adding the extra e2e concurrency coverage.

---

## 💻 Code

```ts
expect(messageIds).toStrictEqual(["assertionOutsideAssert"]);
expect(messageIds).toStrictEqual(["setupAfterAct"]);
```

```ts
const [broadCastResult, incrementalResult] = await Promise.all([
  runFix(broadCastInput),
  runFix(incrementalInput),
]);
```

---

## 🧪 Notes

- Edge cases handled:
  - The AAA expectation tests now fail only when the specific current diagnostic changes, instead of passing for any failure mode.
  - The mirrored e2e typed helper now has overlap-safety coverage, not just the unit helper.
  - The unit typed helper timeout now matches the current full-suite runtime envelope.
- Reproduction/regression checks:
  - Full `npm run validate` succeeded on the exact workspace state after these edits.
  - A second full `npm run validate` succeeded to confirm determinism.
- Assumptions:
  - Keeping these AAA checks as explicit current-behavior tests is preferable to permissive expected-failure tests until the future behavior is intentionally implemented.
- Limitations:
  - The typed prefer-vitest suites remain some of the slowest tests in the repository; this pass improves coverage quality and stability rather than changing the underlying runtime profile.
- Validation performed:
  - `npm run validate`
  - `npm run validate`

## 🔁 Handoff

- Next: Reviewer Agent / human
- Status: ready-for-handoff
